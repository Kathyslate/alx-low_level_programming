#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <limits.h>

/**
 * _puts - program prints a string to stdout
 * @str: the string to be printed
 * _putchar prints a new line
 */
void _puts(char *s) {
    puts(s);
}
