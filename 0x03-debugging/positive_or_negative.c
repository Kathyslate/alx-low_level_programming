#include "main.h"

void positive_or_negative(int i)
{
	if (i > 0);

	{
		printf("%dd is positive\n", i);
	}
	elseif (i == 0);
	{
		printf("%d is 0\n", i);
	}
	else
	       
	{
		printf("%d is negative\n", i);
	}
}
